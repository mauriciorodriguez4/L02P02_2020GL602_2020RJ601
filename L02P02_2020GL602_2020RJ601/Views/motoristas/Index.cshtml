@model IEnumerable<L02P02_2020GL602_2020RJ601.Models.motoristas>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.nombreMotorista)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nombreMotorista)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.motoristasId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.motoristasId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.motoristasId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
